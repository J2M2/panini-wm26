# Personal album state (SQLite DB, exports, derived matrices — keep local only)
data/*.sqlite
data/*.sqlite-wal
data/*.sqlite-shm
data/registry.sqlite
data/albums/*.sqlite
data/albums/*.sqlite-wal
data/albums/*.sqlite-shm
data/*.db
data/panini_snapshot.json
data/matrix.csv
data/frozen/*.json

# Snapshot exports at repo root (never commit)
panini_snapshot*.json

# Raw spreadsheet dumps before import (repo root)
panini_raw*.csv

# Python
.venv/
__pycache__/
.pytest_cache/
*.pyc

# Web UI (Vite)
web/node_modules/
web/dist/
web/tsconfig.tsbuildinfo
